import fs from "fs";
import { randomUUID } from "crypto";

const databsejsonPath = "../database.json";
const databsecsvPath = "../database.csv";

const productController = {

    createproduct: 1,
}

export default productController